{
  "name": "tinytasks-api",
  "version": "1.0.0",
  "description": "A tiny task-list API — demo repo for Docent",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
